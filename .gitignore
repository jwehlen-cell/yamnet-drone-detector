# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python
.venv/
venv/
env/

# Data: only the download script is tracked
data/*
!data/.gitkeep

# Internal field audio (not for redistribution; regenerable via the scripts).
# The embedding/pull scripts ARE tracked; the raw audio + metadata are not.
detection-audio/
detection-metadata/
usafa-dfec-dataset/
usafa-dfec-dataset-16k/

# Build artifacts
build/
dist/
*.log

# Training artifacts (the final .keras and .tflite files in /models ARE tracked)
checkpoints/
runs/
*.png.tmp

# Secrets / local config (QST API credentials, etc.)
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
